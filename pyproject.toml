[project]
name = "point-counter"
version = "0.1.0"
description = "Discord bot for tracking points given between users"
requires-python = ">=3.12"
dependencies = [
    "discord-py>=2.3,<3.0",
    "python-dotenv>=1.2.2",
    "sqlalchemy>=2.0",
]
